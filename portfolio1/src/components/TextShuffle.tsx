// TODO: create a setInterval function here to shuffle job titles, currently: Software Engineer & Web Master
/**
   * const jobTitles = [
      'Software Engineer',
      'Full-Stack Software Engineer',
      'Cybersecurity Engineer',
      'Webmaster',
    ];
    const circularJobTitleArray = jobTitles.concat([...jobTitles]);
    let setSubHeadingChange;
    // convert jobTitles to a circular array and loop over it
    for (let i = 0; i < circularJobTitleArray.length; i++) {
      setSubHeadingChange = circularJobTitleArray[i];
    }
    return setSubHeadingChange;
   * 
   * 
   * 
   */
